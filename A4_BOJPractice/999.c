//https://www.acmicpc.net/problem/999 (문제 번호)

//(문제 설명)
//BOJ에 999번 문제는 존재하지 않는다.
//이 소스 파일은 그저 소스 코드의 기본적인 레이아웃을 나타내거나
//또는 문제를 풀다가 임시적으로 연습용으로 쓰라고 만든 것이다.

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	FILE* f;
	if ((f = freopen("input.txt", "r", stdin)) == NULL);
	{
		printf("File Open Error");
		exit(1);
	}



	return 0;

}